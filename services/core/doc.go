// Package core is the deterministic Go core and API gateway for market-ops:
// the single binary that owns money, identity, observation, events, policy,
// approval, execution, reconciliation, audit, and scheduling (PRD §19.3).
//
// This file is an S1 scaffold placeholder so the module compiles and tests
// green. Real packages (cmd/core, internal/*, migrations, queries) and the
// gen/go + gen/dkgo require/replace wiring land in later steps.
package core
