"""LLM plane package (S1 scaffold placeholder).

The real FastAPI service — intents, context resolution, read/Draft-only tools,
envelope, briefing, orchestrator, providers, evals — lands in later steps. This
placeholder exists so `uv sync`, pytest, ruff, and strict mypy run green.
"""

__all__ = ["__version__"]

__version__: str = "0.0.0"
