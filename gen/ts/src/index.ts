// PLACEHOLDER — regenerated by `task contracts:generate` in S4.
// gen/ is COMMITTED but never hand-edited once generation is wired; this stub
// only exists so gen/ts is a valid pnpm workspace member (referenced
// workspace:*) before the OpenAPI generators land. Real content: openapi-typescript
// schema types + a thin openapi-fetch client (dk-p0-monorepo.md §4).
export const generated = false;
