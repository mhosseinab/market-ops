# openapi-python-client config for the gateway Python client (plan §4.3, monorepo §4).
# Generated, committed, never hand-edited. Regenerated by `task contracts:generate`.
# Pinned generator: openapi-python-client 0.29.0 (invoked via `uvx ...@0.29.0`).
project_name_override: gen-python
package_name_override: gateway_client
