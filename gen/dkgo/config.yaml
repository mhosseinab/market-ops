# oapi-codegen config for the DK Seller API CLIENT (plan §4.3, monorepo §4).
# Generated, committed, never hand-edited. The INPUT is a temporary spec-valid
# document produced by `contracts/tools/dknormalize` from the FROZEN doc
# `docs/DK Marketplace - Open API Service.yml` (never edited; re-frozen only
# deliberately). Regenerated by `task contracts:generate`.
# Pinned generator: oapi-codegen v2.7.2 (invoked via `go run ...@v2.7.2`).
package: dkclient
output: dkclient.gen.go
generate:
  client: true
  models: true
