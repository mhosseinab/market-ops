# DK Marketplace Intelligence P0 — Orchestration Progress (`dk-p0`)

**Durable state for the orchestrator.** On start/resume, READ this file to know where you are.
**PR: https://github.com/mhosseinab/market-ops/pull/2** tracks branch `claude/dk-p0-orchestrator-788qtr` (created from Claude Code UI 2026-07-17). Pushing to that branch updates the PR — do NOT open another PR.
**⚙️ CI-FIX PROTOCOL (user standing order 2026-07-17 "always fix ci in subagent"): on ANY PR CI failure, do NOT fetch job logs into the orchestrator context. Dispatch a `general-purpose` subagent (worktree, reset-first) that: (a) uses GitHub MCP (`mcp__github__get_job_logs` etc.) to fetch the failed-job logs itself, (b) diagnoses the root cause, (c) fixes in `.github/workflows/*`/Taskfile/etc., (d) verifies (actionlint + reproduce the CI condition locally, e.g. `GOWORK=off task go:test` on isolated DB), (e) commits, reports ONLY the SHA + a terse diagnosis. Orchestrator merges by SHA, pushes both branches. Keep orchestrator context clean — never read logs/diffs itself. Git identity is set (user.email noreply@anthropic.com, user.name Claude); commits are UNSIGNED (no signing key in env) so GitHub shows "Unverified" — cosmetic, unavoidable, does not block CI/merge.**
Source script: `dk-p0-implementation-steps.md` (S1..S36).
Worktree: per-step Agent worktree isolation · Orchestrator working tree is ON branch `dk-p0/main` (the integration branch); final deliverable = designated branch `claude/dk-p0-orchestrator-788qtr` (kept ≡ `dk-p0/main`). (Stale `dk-p0/int` @ e121c1f is an abandoned pointer — ignore.)
**Branch mechanics (env constraint): `git checkout`/`switch` is classifier-BLOCKED, and `git branch -d/-D` was blocked too. Never switch HEAD. HEAD stays on `dk-p0/main`. Integrate a finished worker by `git merge <worker-branch>` (workers create `dk-p0/S<N>` inside their own worktree; the branch + its commit SHA are visible in the shared repo) into `dk-p0/main`, then `git branch -f claude/dk-p0-orchestrator-788qtr HEAD`, then push BOTH: `git push origin dk-p0/main` AND `git push origin claude/dk-p0-orchestrator-788qtr` (both branches live on origin at the same SHA — user requested dk-p0/main be pushed too, 2026-07-17). NEVER run `git branch -f dk-p0/main …` while HEAD is on it (fatal). Note: lefthook git hook is installed in shared `.git/hooks` but lefthook binary isn't on the base-tree PATH, so orchestrator doc commits print a harmless "Can't find lefthook in PATH" warning and still succeed.**
**⚠️⚠️ WORKTREE-BASE PROTOCOL (root cause of S4 attempts 1&2): Agent worktree isolation provisions the worker's worktree off the RAW BASELINE `acce0c7`, NOT off dk-p0/main — regardless of the orchestrator's current HEAD. `acce0c7` is a strict ancestor of dk-p0/main with zero unique commits, so it is a safe fast-forward. EVERY worker packet MUST instruct, as the worker's FIRST action: `git reset --hard dk-p0/main` (authorized: the worktree branch has no unique commits; this only advances a stale base to include merged steps) and then confirm the expected prior-step files exist before building. Without this, workers on file-dependent steps correctly STOP with a base-mismatch blocker. Merge workers BY SHA from their handoff.**
**⚠️ HEAD-DRIFT GUARD (learned S4-attempt-1): dispatching a worktree-isolated worker can leave the ORCHESTRATOR tree's HEAD on a `dk-p0/S<N>` branch. ALWAYS run `git branch --show-current` and confirm it prints `dk-p0/main` BEFORE any merge/commit. If it drifted: `git branch -f dk-p0/main <good-sha>` then `git symbolic-ref HEAD refs/heads/dk-p0/main` (symbolic-ref IS allowed; `git checkout`/`switch`/`branch -d`/`-m` are classifier-BLOCKED). To recover a merge that landed on the wrong branch: advance dk-p0/main to that merge SHA, symbolic-ref HEAD back to dk-p0/main. Merge workers BY SHA (from their handoff), not by branch name.**
Review routing: canonical capability roles per plan §4.6 and `dk-p0-agent-guidelines.md` §8 — `contract-data` (contracts/gen) · `connector-observation` (connector/catalog/identity/observation/routec) · `cost-readiness` (cost/margin readiness) · `domain-execution` (money/policy/approval/execution/audit/outcome) · `llm-plane` (services/llm) · `web-surface` (apps/web) · `extension-surface` (apps/extension) · `locale-qa` (locale/copy/RTL/Persian fixtures) · `reliability-delivery` (deploy/CI/observability) · **`invariant-review` at phase boundaries (after S7, S19, S24, S29, S31, S33) and all gated steps** · `delivery-lead` for schedule/descope calls. Resolve roles through the active runtime adapter crosswalk; do not persist vendor profile names as ownership semantics.

## Rules in force
- Respect the dependency graph (steps doc): start a step only when its prerequisites are `passed`; run independent steps in parallel; **never run two [C] steps concurrently** (all touch `contracts/` + `gen/`); never let two steps edit the same file concurrently.
- Cap 3 review cycles per step → on the 3rd failure FILE A GITHUB ISSUE (`gh issue create`, title `dk-p0 S<N> blocked: <title>`, labels `dk-p0` + `blocked-step`; fallback: append to `dk-p0-issues.md`) carrying the reviewer's outstanding findings verbatim (file:line), final Verify output, branch/SHA, attempts, suspected root cause, and the change requests needed — then mark the step `blocked` with the issue ref and CONTINUE with steps not (transitively) depending on it. Dependents stay ineligible; never merge a red branch.
- Still STOP (don't file-and-continue) when: a never-cut invariant would be weakened, a product decision is needed, a hard gate (S34/S35/S36) is reached, or no eligible work remains. All open blocked-step issues resolve to `passed` or are explicitly human-descoped BEFORE S36.
- Fan out every eligible step concurrently at each scheduling point (four plane-chains + [C] mutual exclusion); the orchestrator reads no diffs/files/logs itself — subagents report, the orchestrator records and compacts.
- STOP and require an explicit human "go" before: **S34** (live deploy), **S35** (live production probes + per-write-approved reversible price writes + paid model benchmark), **S36** (human sign-off).
- Never auto-run paid/live operations (deploy, production migration/write, paid eval, secret rotation).
- PRD §4.6 never-cut invariants override any convenience; a genuine PRD gap escalates to the human — the PRD is final, workers don't improvise product decisions.

## 🖥️ Environment (host state — re-establish after any container recycle)
- **Egress is allowlisted to language package registries only** (npmjs, pypi, files.pythonhosted, crates, proxy.golang.org). General web + Docker image CDNs (Docker Hub `production.cloudfront.docker.com`, GHCR `pkg-containers.githubusercontent.com`) + `www/apt.postgresql.org` + `mcp.context7.com` are policy-DENIED (403). ⇒ **no Docker image can be pulled/booted; PG18 cannot be fetched.** Do not route around it (per /root/.ccr/README.md).
- **Native PostgreSQL 16.13** installed & running (cluster `16/main`, port 5432, human-authorized 2026-07-17 in lieu of Docker/PG18). App role `market_ops` (LOGIN, **CREATEDB** granted so it can drop/recreate its own DB via the `postgres` maintenance DB — mirrors the compose superuser) owns db `market_ops`. Workers needing a DB must use: `DATABASE_URL=postgres://market_ops:market_ops@localhost:5432/market_ops?sslmode=disable` (pass in every DB-step packet; server persists on host, not in worktrees). Restart after recycle/stop: `pg_ctlcluster 16 main start` (OBSERVED 2026-07-17: server stopped mid-run → 'connection refused'; restart cleared a stale pidfile; data persisted, role/db intact — a DOWN server manifests as DB-test/migrate failures, always check PG is up before diagnosing a merge as broken). **Agents must NEVER use `sudo`/superuser or raw `DROP DATABASE` outside `task db:reset`; go through `$DATABASE_URL`.**
- **⚠️ SHARED-DB POLLUTION (recurring): multiple concurrent worktree workers running `goose`/`task db:reset` against the SAME `market_ops` DB collide (leftover FK-dependent objects → spurious `migrate:verify`/`db:reset` failures; observed S7-fix, S9, safety review).** MITIGATION for all DB-step packets going forward: instruct the worker to use a UNIQUE scratch DB, e.g. `export DATABASE_URL='postgres://market_ops:market_ops@localhost:5432/market_ops_<STEPID>?sslmode=disable'` — the role has CREATEDB and `task db:reset` derives+creates the DB name from the URL, so each worker gets an isolated DB. Orchestrator merge-verification: run `task db:reset` immediately before `migrate:verify`, ideally against a dedicated verify DB, and only when no other DB worker is active. Do NOT treat a shared-DB migrate flake as a step failure — re-run isolated.
- **Context7 MCP unreachable** (egress-blocked) — workers validate third-party behavior empirically instead; note this in handoffs.
- **⚠️ Fresh worktrees lack `node_modules`** → `task ci:local`/`contracts:drift` fail at `contracts:gen:ts` (openapi-typescript binary absent). Every packet whose Verify runs ci:local/contracts:drift MUST instruct `pnpm install --frozen-lockfile` first. Go-only workers can otherwise run the Go plane + `task lint:money` + `task migrate:verify` directly.
- **⚠️ NEVER leave a `go.work` at the repo root of the orchestrator (main) tree.** Nested worker worktrees walk UP and discover it, causing a worker's `go get`/`tidy` to write into the MAIN tree's `services/core/go.mod` (observed: stray `pgregory.net/rapid` leaked in). For orchestrator merge-verification, build with `cd services/core && go build/test ./...` — the committed `replace` directives (→ ../../gen/go, ../../gen/dkgo) make it work WITHOUT go.work. If you must create go.work transiently, `rm -f go.work go.work.sum` immediately after. go.work is gitignored (never committed); CI uses GOWORK=off.

## ⚠️ Deferred verification gate (run before S36 sign-off)
- S2: Docker-compose runtime boot of the observability stack (otel-collector/grafana/loki/tempo/mailpit/spotlight) + `task dev` exit 0 + PostgreSQL **18.x** version assertion + Spotlight UI :8969 — all Docker-image-gated; run on an unrestricted host. (DB-path verification done locally against native PG16.13.)
- S6: first push to GitHub — all CI jobs green (offline check was `task ci:local` + `actionlint`). **NOW BEING EXERCISED on PR #2.** Run 1 (2d26d58): RED — `detect` job's `dorny/paths-filter@v3` failed `Resource not accessible by integration` (workflow GITHUB_TOKEN lacked `pull-requests: read` for the PR listFiles API; detect gates all jobs → whole run red). Fix da374082 (add `pull-requests: read` to ci.yml top-level permissions) MERGED a3a92a5 + pushed → CI re-run triggered on a3a92a5. Subscribed to PR #2 activity + ~1hr self-check-in (trig_01LAxRZZVENFD3Ht3BKCzF9v). Iterating CI to green. Run 2 (b93c228): detect PASSED (perms fix worked), pg18 service container came up (PG18.4). NEXT RED: `go` job — `go:test`'s `go:init` dep runs `go work init/edit` which fails under CI's `GOWORK=off` ("no go.work file found"). Local ci:local masks it (doesn't set GOWORK=off). Fix a707ae5 (go:init `status:` short-circuits under GOWORK=off) MERGED 5ea1024; verified GOWORK=off go:build/go:test pass (all pkgs ok). Pushed → CI run on 5ea1024. Run 3 (eff602b): go job got past GOWORK, then FAILED — DB tests hit "relation organizations does not exist" (go:test ran before schema applied). Fix 18d9fd9 (`task db:reset` before `go:test`) merged. **✅ CLEARED: CI fully GREEN on 71aadfc (S20 merge) — both workflow runs `success`; ALL jobs pass on GitHub (detect/contracts/go/py/ts/money-guard incl. semgrep + pg18 service container).** S6 first-GitHub-run deferred gate SATISFIED. CI-fix iteration log: detect-perms(da374082)→GOWORK-off(5ea1024)→schema-order(18d9fd9), each a GitHub-only gap the offline check couldn't catch. (get_status shows "pending/0" because workflow uses check-runs not commit-statuses — the RUN conclusion is the source of truth.) (go:lint passed 0 issues; two concurrent go-job runs from push+PR triggers — same failure, dedup.) NOTE: workflow has a `concurrency` group → avoid extra PR-branch pushes while a run is in-flight.
- S24: paid model-provider benchmark via the eval harness — select the lowest-cost qualifying provider pair; record P75 cost (needs budget authorization).
- S34: first production deploy + backup restore drill (human-witnessed).
- S35: all PRD §4.1 Gate 0a live measurements on ≥3 production accounts; per-threshold pass/fail + PRD-decided consequence recorded in plan §11.
- S35 (from S9 review): the connector probe fires an EMPTY-BATCH price_write POST on every connect/refresh — offline/mock it's inert, but before any LIVE connect confirm an empty batch is genuinely non-mutating & non-billable on real DK, and consider gating the write probe behind explicit opt-in. price_write stays capped at Degraded until a human-approved reversible write probe.
- S32: §16 edge-case rows not automatable offline — list them here when S32 lands, with manual-test owners.
- **LOC-003 native-Persian copy sign-off (RELEASE GATE, §20 beta — beyond S36 internal alpha):** all authored non-glossary Persian copy (nav/route subtitles/messages across S25–S29) needs a real native Persian operator sign-off before private beta. locale-qa proxy reviews can't discharge it. Accumulate the copy surface as web screens land; requires human native reviewer.

## Open blocked-step issues
> One line per issue filed under the 3-cycle policy: `S<k> — <GitHub issue URL or dk-p0-issues.md#id> — <one-line reason> — dependents held: <steps>`. Mark resolved when the step re-runs to `passed` or the human descopes it in the plan. MUST be empty before S36 sign-off.
- *(none)*

## Status table

| Step | Title | Status | Attempts | Branch | Commit SHA | Note |
|------|-------|--------|----------|--------|-----------|------|
| S1 | Scaffold monorepo + rules doc | passed | 1 | dk-p0/S1 | fd58883 | PASS 1st cycle (reliability-delivery); merged. CF: forbidigo float ban is repo-wide (.golangci.yml) — S5/S7 must keep money-path guard when scoping; Go core has no smoke test (per-spec); postgres-mcp `--access-mode=restricted`; generator tools (oapi-codegen/goose/sqlc) unpinned → S4 pins per monorepo §4 |
| S2 | Dev stack (PG18 + otel compose) | passed | 2 | dk-p0/S2 | ee97605 | PASS cycle 2 (reliability-delivery); merged into dk-p0/main @ c49a972 (merge resolved .env.example conflict w/ S3 = keep both). db:reset Docker-free via $DATABASE_URL (verified vs native PG16, fails closed). CF/DEFERRED GATE: Docker-only stack boot (otel/grafana/loki/tempo/mailpit/spotlight) + `task dev` + PG**18.x** assertion + Spotlight UI → unrestricted-host gate pre-S36 (Docker+apt.postgresql.org egress-blocked here; native PG16.13 substitutes for DB path only) |
| S3 | Go core service skeleton | passed | 1 | dk-p0/S3 | 2bf32cf | PASS 1st cycle (reliability-delivery), reviewer re-ran full Verify; merged 9f4178e. CF: (a) add gen/dkgo→internal/connector depguard rule when those land (S9); (b) gen-go-boundary currently excludes cmd/** too — tighten to httpapi-only if router wiring lives in httpapi (S4); (c) ldflags build-info injection deferred to release tagging; (d) OTEL_ENABLED=true + Sentry-enabled positive tests deferred until a collector/sidecar fixture exists |
| S4 | Contracts pipeline + drift check [C] | passed | 1 | worktree-agent-aa8891ee | b9d2997 | PASS 1st review cycle (contract-data; 2 earlier stale-base aborts don't count). Merged f731790 (resolved go.mod union w/ S5; orchestrator verified merged core builds+tests green + go mod tidy clean). CF: (a) gen/ts/src/{client,index,client.test}.ts are prompt-sanctioned hand-authored under gen/ (only schema.d.ts regenerated) — don't flag as drift; (b) go directive skew across gen modules (cosmetic); (c) oapi-codegen prints 3.1 "not fully supported" warning but emits correct code; (d) gen/dkgo favors compilability over precise typing — connector step verifies concrete shapes it needs |
| S5 | DB foundation (goose+sqlc+River) | passed | 1 | worktree-agent-a100671f | 2fb7ee7 | PASS 1st cycle (reliability-delivery), reviewer re-ran full Verify (down-path, sqlc drift-free, River rollback-discard genuine+skip-guarded); merged f9d324a. CF: (a) users/marketplace_accounts `updated_at` set on insert but not maintained — later mutation steps MUST bump it; (b) River CLI now a `task doctor` dep — S6 CI must provision goose+sqlc+river; (c) observation-table partitioning deferred to the observation schema step (S13); (d) go directive 1.25.0, toolchain auto-DL 1.25.12 |
| S6 | CI pipeline | passed | 2 | worktree-agent-ac8a268b | 138b85e | PASS cycle 2 (reliability-delivery); merged 675cdce (clean, actionlint green on merged tree). Cycle 1 caught golangci-lint v1.64.8↔version:"2"/go1.25 CI drift; cycle 2 pinned v2.5.0 (local↔CI parity). CF: **first-GitHub-run = DEFERRED gate** (Actions egress-blocked); go job pg18 service container GitHub-only; ci.yml install.sh tag-fetch needs GitHub egress. ci:local order groups by phase (faithful mirror, not per-job) |
| S7 | Money type + static guard | passed | 2 | worktree-agent-a073ca7a | 78438d4 | PASS: domain area (cycle1) + phase-A safety (cycle2). Merged 7802f1a — **PHASE A CLOSED (S1–S7)**. Merged-tree verified: money tests ok, lint:money guard clean+fires, money-guard CI job present, actionlint 0. CI money-guard job installs semgrep 1.170.0. Non-block follow-ups for later: §9.1 card-vs-approval pkg naming, forbidigo analyze-types, nosemgrep rule-scoping+CI-grep confinement (S16/S17). |
| S8 | AuthN/Z + shared permission matrix [C] | passed | 2 | worktree-agent-a31d9843 | c731fe4 | PASS: domain area (cycle1) + security (cycle2 after default-deny fix). Merged 3ee38b7 (sqlc-regen resolved models.go/querier.go conflict w/ S10; verified all Go tests + migrate 0001-0004 + drift 0 + tidy clean). perm.Can ONE matrix (chat+screens), fail-closed middleware (default-deny unlisted, structural route-coverage test), argon2id+SHA256 sessions. Ships dark. CF: S12/S16/S17/S20 must authorize via perm.Can; login-timing asymmetry P0-accepted. | worker GREEN: ONE perm.Can matrix (chat+screens, must-deny coverage per role×L1-L4), argon2id (m=19MiB) + server-side sessions (256-bit token, Secure/HttpOnly/SameSite cookie, only SHA-256 stored, never in body), fail-closed middleware every non-public route, contract additive (login/me/logout+cookieAuth, drift 0), migration 0003 reversible, ci:local exit 0. Ships dark. Review: safety_release SECURITY = CHANGES_REQUESTED — BLOCKER (High design): perm middleware FAILS OPEN on unmatched path (routePolicies miss → passthrough); guarantee is hand-synced test, not structural → future route could ship unprotected. Fix: DEFAULT-DENY unmatched non-public paths OR derive test route-set from generated mux. +optional: DB-row argon2id-prefix assert; login timing asymmetry. Creds/sessions/ladder verified SOUND. Domain area review = PASS (flagged same middleware issue as OPTIONAL). Both agree on finding; treated as BLOCKER (authz boundary = fail-closed invariant). Fix cycle 2 = c731fe4 (parent f7329bc): default-deny middleware (unlisted→401/403, no passthrough) + structural route-coverage test via recording ServeMux (demonstrated FAILs on missing policy) + argon2id DB-row prefix assert. No contract/gen change (drift 0), ci:local exit 0. In SECURITY re-review. Merge BY SHA c731fe4 onto current main (S10+S25 merged) — resolve go.mod union. CF: matrix pre-declares later-step actions (S12/S16/S17/S20 must authorize via perm.Can); new `session` contract tag (additive). Merge overlap w/ S10: go.mod + migration numbering (0003 vs 0004). Merge BY SHA f7329bc |
| S9 | Connector capability layer + mock DK [C] | passed | 2 | worktree-agent-ad22cd23 | 7e9e380 | PASS: connector area (cycle1) + security (cycle2). Merged 302fd6e (NO conflict — ort auto-merged go.mod; verified build+tidy+connector/money tests on isolated DB). Recorder redacts token bodies+refuses auth-exchange. CF (go_connector_observer, defense-in-depth): bare top-level JSON scalar body written verbatim (mitigated by auth-path refusal); record redacts only Authorization header (bearer API, ok). Prior CF: gen/dkgo nil-interface→raw ClientInterface; empty-batch write probe→S35 note; token zero-expiry→S10+. |
| S10 | Catalog + owned-offer sync | passed | 1 | worktree-agent-a0da8ff9 | 4ceaa33 | PASS 1st cycle (connector-observation), reviewer re-ran all Verify isolated (UNIQUE constraints DB-backed, reorder-zero-dup, resume, append-only snapshots, RawAmount-only no Money, reconciliation report-only, sqlc clean). Merged 0383220 (clean, no go.mod change; migrate up/down/up to v4 verified). CF: catalog.RegisterWorkers not wired into cmd/core (bootstrap step, later); products.brand_title empty until enrichment; RawAmount Text==Value token (revisit if verbatim price string surfaces); per-item update-counter misattribution (bookkeeping only) |
| S11 | Identity mapping [C] | passed | 1 | worktree-agent-a220aa94 | 64a59b5 | PASS (connector-observation), reviewer re-ran identity-quarantine query-layer + 23505 + append-only + gateway hygiene. Merged 6a47b52 (clean; verified all go tests + migrate 0006 + drift 0 after restarting PG). CF: dead dedup-recovery branch service.go:229-240 (unreachable, later cleanup); OBS-001 targets source from ListConfirmedObservationTargets (S13/S14). Unblocks S12[C]/S13[C]/S30 |
| S12 | Cost profiles, CSV import, margin readiness [C] | passed | 1 | worktree-agent-abd499feb50f607d1 | dfa40f6 | worker dfa40f6 (isolated). DUAL review BOTH PASS cycle1 (domain-execution area + safety money). Migration 0008 cost_profiles (component-versioned effective-dated append-only) + margin_readiness (Complete|Partial|Stale|Missing). internal/cost CSV import: preview-before-commit STRUCTURAL (DB CHECK reason-required + SQL guard status=preview AND duplicate_count=0), §16 dup rows block commit, CST-002 point-in-time lookup (<= inclusive boundary), CST-003 data-driven readiness (hardRequired+sku_cost_requirements+account_cost_policies, precedence Missing>Stale>Partial>Complete). Money integer-only (ParseAmount string-assembly+ParseInt, rejects >int64/over-precision/negatives, no float), LOC-007 Persian==Latin property. Cost display-only, NO execute/approve consumer (held S16+S35); Toman transform NOT enabled. perm default-deny (cost.import/config.single_cost_value L2 Internal-denied; read.cost_readiness L1). Contract additive drift 0, 0008 reversible. Merged 25fc1e8 (merge-verify: drift 0, build OK, migrate→8, cost/httpapi/perm/normalize tests pass). Frees [C] slot → S15[C]/S16[C]. CF: (1) SYSTEMIC int64 money mantissa renders JS `number` (>2^53 precision) → api_data_contracts: string-encode int64 mantissa on any JS-read path; (2) readiness recompute must cascade when policy/applicability endpoint is wired (later step); (3) principal→account tenant binding at platform layer; (4) lenient grouping-sep stripping (input hygiene). |
| S13 | Observation store + quality states [C] | passed | 2 | worktree-agent-a393d340809895aa2 | 96dd3e3 | worker ea38280→fix 96dd3e3. DUAL review 2 cycles BOTH PASS (connector-obs area + safety never-cut). Migration 0007 (observations PARTITIONED by capture month, append-only, 25 partitions), §10.3 six-state machine, OBS-001 trigger unbypassable (3 raw-insert probes rejected 23514), route-keyed dedup replay-safe, OBS-004 expiry, §16 disappearance-close never-zero. Cycle1 3 BLOCKING fixed in cycle2: (1) Route B upload now rejects native_variant mismatch (ErrIdentityMismatch→409, identity_valid server-derived, client confidence downgrade-CAP only, schema_valid from allow-list not client) — identity quarantine fail-CLOSED; (2) corroboration derived from append-only in-window evidence (ListInWindowRouteValues freshness_deadline>now, DISTINCT ON route) — Verified requires corroborator's OWN in-window obs, stale route drops from provenance (no more out-of-window Verified); (3) route disagreement→Conflicted w/o overwriting price, gate blocks (§16 honored). First-sighting→Unverified (strict §10.3). 0007 comment corrected (no false retire claim; S14 owns identity-reopen retire via 0006 invalidation events). Money raw-only. perm additive (read.observations L1; observation.upload_capture L2 Owner/Operator, Internal excluded from ingest). Merged 6120acd (merge-verify: drift 0, build OK, migrate→0007, obs/httpapi/perm tests pass). CF: same-route 2 in-window captures→Supported (recommend-capable, gated by JIT downstream) — confirm intent at S14; target deactivation on identity-reopen deferred to S14. |
| S14 | Route C observer + scheduler + parser fixtures | passed | 2 | worktree-agent-ad748a8b1710da0a5 | 9cb537d | worker 9a265c6→fix 9cb537d. Reviews: safety PASS cycle1 (all invariants held: identity-quarantine deactivation=flag-flip preserves append-only, OBS-007 skip paths, Route C no-auth/single-endpoint/no-enum, kill switch durable, drift 3-flag recovery); connector-obs area cycle1 CHANGES_REQUESTED→cycle2 PASS. 2 BLOCKING fixed: (1) backoff was dead config → wired fetchWithRetry bounded transient-only retry (SignalTransport/5xx, MaxRetries=3, each retry re-checks breaker.Allow()+own budget.Reserve()+backoff wait, single held slot, no kill-switch/OBS-007 bypass, failed final→SkipFetchSignal no ingest); (2) budget monotonic permanent-starvation → injected clock + 24h window (§17.3) deterministic reset, within-window still blocks, restart only frees. +host-pin CheckRedirect refuses cross-host redirect (docs/12). internal/routec full (per-signal breaker 403@3/429@2/challenge@1/latency@5/drift@1, layered kill switches, River scheduler tiers reduce-before-widen, parser golden /v2/product/{id}/ only, drift→pause/downgrade). S13 CF resolved: TargetRetirer deactivates reopened-identity targets. Migration 0010 route_kill_switches, NO contract change. Merged bab6853 (sqlc querier.go conflict w/ S12 cost resolved via regenerate; merge-verify migrate→10, build OK, routec/observation/cost tests pass, drift 0). CF: observer/River/TargetRetirer tested seams not booted in main (later platform step); breaker per-account in-memory (per-host follow-up); MeasuredPriorityCap 0→50 until S35; pinHostRedirect mutates supplied client field (P0 ok). |
| S15 | Event engine + Today ranking [C] | passed | 2 | worktree-agent-ace0dcae4f1359aae | 810277f | worker e096d91→fix 033dc29 (isolated). [C] slot; migration 0011 (above merged set). DUAL review: SAFETY co-review PASS cycle1 (never-cut structurally verified vs live-PG16 direct-SQL probes — dedup partial-unique-index rejects dup raw INSERT; EVT-005 two CHECKs reject numeric-when-unknown + known-without-currency; no-float; append-only tables no UPDATE/DELETE; free-text note never reaches Rank(); read.events L1 + relevance L2 not approve/execute). DOMAIN area review CHANGES_REQUESTED cycle1 (core CORRECT — ranking big.Int/no-float, 5 detectors, dedup, EVT-005 struct, 0011 reversible, perm wiring all pass) → single BLOCKER missing httpapi 503-fail-closed negative test → fix cycle2 added event_test.go (503 for GET events/event/today + POST relevance unwired + wired mapping incl EVT-005 absent-number + principal-not-body) + optionals (movementBp big.Int, ChallengeMarginBp documented, GetEvent 404-vs-500) → DOMAIN PASS cycle2. EVT-001..005: 5 detectors (floor dormant unless readiness Complete), append-only versioned materiality thresholds (point-in-time reproducible), structural dedup (partial unique index → 0 new rows on open/update), big.Int rank exposure×confidence×urgency deterministic tie-break, EVT-005 Unknown structurally≠zero. Merge 810277f (--no-ff; gen/go conflict w/ #58 resolved by regenerate; drift 0). Pre-push merge-verify ALL GREEN (contracts-drift 7s, go-lint 47s, go-tidy, mypy). 2 non-block product-ranking notes → product_delivery_lead: Unknown-exposure band ranks below ALL known regardless of severity; muted not wired to feed. Frees [C]→S17[C]. |
| S16 | Contribution + policy engines [C] | passed | 1 | worktree-agent-a8da2de4f254a4aaa | fa8c41d | worker fa8c41d (isolated, resumed after session-limit, rebased onto 2355800). DUAL review BOTH PASS cycle1 (domain-execution area + safety money/policy). internal/margin contribution §9.2 (net proceeds−COGS−commission−fulfillment−shipping−packaging−promotion−ads−returns) Money/basis-point ONLY (math/big integer QuoRem, no float), named/versioned round-half-even@v1 single rounder, consumes cost-profile versions (CST-002) + readiness (only Complete Executable()), currency/exponent mismatch rejects. internal/policy 6-stage §9.3 (boundary→hard floor→cap→cooldown→strategy→objective); strategy/objective only set desired price clamped to boundary∩cap then floor+zero-guard LAST → no later stage overrides hard (PRC-003); zero-cross computed INDEPENDENT of floor (≤0 floor still can't admit non-positive). rapid property tests 10k/0-counterexample (ordering + zero-floor, generators genuinely produce sub-floor/over-cap/zero-cross attempts). PRC-004 cap>500bp/cooldown<60m rejected at construction AND Evaluate. Typed blockers in policy order. Simulation structurally non-approvable (PolicySimulationResult additionalProperties:false, NO approval-shaped field, approvable=false set structurally, handler no arithmetic). perm policy.simulate L1-read all roles, mints no approve/execute. Gate-0a marginreconcile CLI 5 synthetic settlements exact tolerance-0. lint:money covers margin+policy unmodified. Contract additive POST /policy/simulate, drift 0. STATELESS no migration (0009 remains free). Merged 89205b6 (merge-verify: drift 0, money-guard exit 0, build OK, margin/policy/httpapi/perm tests + 10k props pass). Frees [C]→S15[C]. CF (→S17): monotone-contribution precondition (feasHigh=max-contribution assumes monotone; safe-but-suboptimal if non-monotone oracle wired), real net-proceeds oracle (P0 NetProceeds=RateBase=price), wider cooldown generator. |
| S17 | Recommendations + approval machine [C] | passed | 1 | worktree-agent-a3168ab0812a38261 | 1600923 | worker 20f6f78 (isolated, based 7982837). DUAL review BOTH PASS cycle1 (domain-execution area + safety never-cut). internal/approval: §8.4 transition table verbatim (state.go:131-148 vs PRD 543-559), state×state sweep accepts exactly PRD edges + rejects all else ErrUndefinedTransition, fail-closed unknown endpoints, terminal set pinned. APR-001 Binding binds action id+parameter+context+policy+cost-profile+evidence versions+expiry; ValidateAgainst checks expiry-first then every dim, returns exact changed reason; per-dim matrix test mutates real binding (evidence bump/add/remove) not hardcoded map. CHAT-044 EditPrice mints new card+parameter version, resets Draft, price never mutated in place, stale control→ReasonParameterChanged. PRC-001 Optional[T] present-or-unavailable-with-reason structural (per-field *_available/*_reason cols+CHECK); PRC-002 seven blockers→Approvable false→NO binding/card/expiry, negative suite proves controls=0 across all 7 + simulation. Idempotency key action:<id>:pv:<pv> stable, UNIQUE(idempotency_key) on approval_cards (0012:175). Append-only: approval_card_states INSERT-only; projection UPDATE FROM-guarded optimistic (WHERE id=$1 AND state=$2 RETURNING) → stale from-state matches no row, advance rolls back. No float on money path; Money methods only; raw evidence preserved separate. LLM plane untouched; confirm/bulk-confirm L4 price.approve (Owner/Operator, machine never); new read.approvals L1. S18 stub Card.Revalidate always ErrExecutionUnavailable naming S18 (fail-closed, negative test); ConfirmApproval on Approved sets ExecutionPending only, NO write. Migration 0012 up+down reverse-dep order, above merged 0011. Contract additive, drift 0. Merge 1600923 (--no-ff, no conflict — origin didn't move). Pre-push merge-verify ALL GREEN (contracts-drift 5.6s, go-lint 0, go-tidy, mypy 42 files). CF→S18 (both reviewers flagged same): confirm-time `current` binding is client-echoed not server-resolved — MUST re-resolve current versions server-side at Revalidating gate before Revalidating→Executing (Approved is non-writing so safe in S17); bulk execution must re-validate each member card (state/expiry/per-card idempotency) not just set-version; correct card.go:133-139 + approval.go:56-58 comments (overstate live re-check). Minor: persist.go applyOptionalRange takes currency/exponent from Range.Min only — assert Min.Currency()==Max.Currency(). Frees [C]→S18[C]/S23[C]. |
| S18 | Execution/reconciliation/audit/outcome [C] | passed | 2 | worktree-agent-ab1d29961121e3503 | bbbcdaf | worker 71ea8f1 (based 5b32d41, holds [C] slot). task ci:local EXIT 0. Both S17 carry-forwards landed (server-side current re-resolution via GetCurrentExecutionContext lineage replacing the stub; per-card individual revalidation). DUAL review cycle1 BOTH CHANGES_REQUESTED — write-BLOCKING mechanisms CONFIRMED SOUND by both (dark-launch two-key gate CapabilitySupported&&RegionWriteVerified, race-safe INSERT…ON CONFLICT(idempotency_key) DO NOTHING claim + UNIQUE, S17 client-echo hole CLOSED [ExecuteActionRequest carries CardId only], EXE-001 pure 9-gate matrix exhaustive, append-only audit/outcome tables, no-float, LLM-plane L4-only ActionExecutePriceChange machine-denied). BLOCKERS: (B1 both) AUD-001 audit append swallows error `_,_=audit.Append(db.New(s.pool))` + NOT atomic w/ state change (fresh pool not tx) — external write/transition can commit w/ NO audit row + no log/metric; comments at audit.go:71-73 + service.go:366-368 FALSE → thread state-change tx into Append, fail-closed on error, emit log+metric. (B2 safety) concurrent idempotency proof MISSING (test sequential-only) — add N-goroutine + retry-races-original asserting writes==1 & one row (S27 confirmed UI double-POST reachable). (B3 domain) EXE-005 recommend-only forward-transitions [Match/SetRecommendOnlyState/ListAwaitingRecommendOnlyForVariant] + §15.3 outcome [ComputeAndAppend/ListClosable] UNWIRED — no prod caller, dead pure logic; recommend-only is ONLY reachable prod mode (writes dark) so this IS default prod behavior; worker misattributed close-job to S19 but S19=Notifications/analytics NOT scheduler → wire River jobs (internal/jobs+River exist), no downstream owner. (B4 safety) NO metric/trace/log on execution path → wire observability seam (write attempt/dedup conflict/gate-block/pending-recon park/enablement-denied + trace ctx action+param+context version). REQUIRED-SMALL R5: DefaultResolver hardcodes 5 gate signals (resolver.go:92-97) — guard CanWrite() vs static-stub signals fail-closed + name S35 + negative test. NON-BLOCK N6 Card.Revalidate dead in prod path (consolidate/assert-agree), N7 recommend-only gate-block leaves card Approved not Invalidated, N8 bulk-execute unwired=acceptable P0. Fix packet → worker cycle1 (resumed). [C] slot HELD until merge. **S35 RELEASE-GATE CHECKLIST (safety sign-off before RegionWriteVerified flipped on): (1) live per-signal resolver replacing resolver.go:92-97 hardcoded identity/price/money-unit/boundary/permission/JIT + fail-closed guard; (2) audit write-path integrity [B1] fixed; (3) concurrent idempotency proof [B2] green vs mockdk; (4) observability seam [B4] wired; (5) HTTPWriter DK batch/variant/update contract + Idempotency-Key header confirmed by GATED reversible-write probe, UpsertWriteVerification reachable only from gated S35 path never runtime handler; (6) scheduled reconciliation drains pending_reconciliation before enabling.** FIX CYCLE1→17d3056 (all 4 blockers+R5+N6/N7 closed): B1 audit now atomic in shared pgx.Tx (AdvanceTx on caller tx; append-error rolls back state change fail-closed + execution.audit_write_failures metric; swallow helper deleted; false comments gone); B2 concurrent idempotency proven (12/10-goroutine simultaneous-release same action_id → writes==1 & one row, under -race×3); B3 EXE-005 recommend-only reconciler + §15.3 outcome closer wired as REAL in-process River periodic jobs (5m matcher / hourly close, RunOnStart, drain-on-shutdown), owned-price/outcome-evidence sources = explicit fail-closed stubs NAMING S35 (lapse/Not-Measurable honest dark behavior); B4 telemetry.go 8 counters+spans(action+param+context version)+logs on write-attempt/dedup/gate-block/pending-park/recommend-only/audit-failure/terminal/enablement-denied; R5 DefaultResolver returns ErrSignalsStatic if write-enabled while gate signals static (WithLiveSignals=S35 opt-in)+negative test; N6 Card.Revalidate agreement test vs Binding.ValidateAgainst; N7 recommend-only gate-block now Approved→Revalidating→Invalidated. DUAL RE-REVIEW cycle2 BOTH PASS (domain verified delta+reran -race; safety verified adversarially — write-succeeds-then-tx-fails is fail-closed [card stays Executing/record pending_reconciliation, never silent accepted-orphan], grep 0 swallowed appends, LLM-plane/perm untouched). Merge bbbcdaf (--no-ff, no conflict — S18 core/gen disjoint from docs+web on main since 5b32d41). Merge-verify: drift 0, migration 0013 ordered, go build clean, money-guard exit 0, pre-push go-lint 0/go-tidy/mypy green. **[C] SLOT FREE.** 24 steps passed (S1-S18,S20,S21,S22,S25,S26,S27). **S35 GATE CHECKLIST (safety sign-off before RegionWriteVerified on): #2 audit-integrity SATISFIED, #3 concurrent-idempotency SATISFIED, #4 observability SATISFIED (this cycle); OPEN: #1 live per-signal resolver (guarded by ErrSignalsStatic), #5 HTTPWriter DK contract via gated reversible-write probe + UpsertWriteVerification only-from-gated-path, #6 reconciliation drains pending_reconciliation — EXTENDED: add Executing-sweep/pending-drain job before writes live (fail-closed holds, unreachable while dark, non-blocking now).** N8 bulk-execute unwired=acceptable P0 (no bulk endpoint). Unblocks: S19[C](S15,S18), S23[C](slot), S28(S26,S27,S18), S30(likely [C]). |
| S19 | Notifications + analytics [C] | pending | 0 | — | — | phase-B boundary: safety review |
| S20 | LLM skeleton + tool registry + kill switch [C] | in_progress | 1 | (worker worktree) | — | dispatched (llm-plane; +SECURITY co-review — Draft-only registry/free-text containment/LLM credential). [C]: extends perm+contract (LLM_GATEWAY_TOKEN) +regen. Migration 0005 (conversations/messages). DB market_ops_s20. LangGraph sole orch + create_agent leaf, confined services/llm; deterministic mock, NO paid CI; money never float in response_format. Chosen 1st [C] (successor S21 non-[C] restores concurrency). worker GREEN 24f54f9: registry read+Draft-only (negative tests: no approve/execute/confirm/guardrail/permission tool; agents' tools ⊆ manifest), perm.GatewayCan read+Draft-only fail-closed, LangGraph StateGraph + create_agent leaf, mock-only provider, kill switch (chat 503 / screens 200), loop bounds→§12.4 failure, money never float, no DB cred in LLM plane, SSE, migration 0005 (conversations/messages, 90d retention, no exec-state) down-verified, contract additive+drift 0, ci:local exit 0 (locally). Review: SECURITY = PASS (containment structurally enforced, independently reproduced). llm-plane area = CHANGES_REQUESTED: 2 complete-seam/truthfulness blockers — (1) per_tool_timeout_seconds is dead config but docstring claims §12.4 enforcement; (2) token-ceiling "no silent truncation→§12.4" only sets max_tokens (truncates silently, finish_reason=length not inspected). +optional #5 exclude surface-only chat.converse from LLM gateway grants. Containment/kill-switch/neutrality intact. Fix cycle 2 = 35e47c9 (parent 24f54f9): per-tool timeout wrap_tool_call→§12.4 TOOL_TIMEOUT (test: pre-fix hangs=dead config, post-fix fails-closed at bound); token ceiling wrap_model_call finish_reason==length→§12.4 TOKEN_CEILING (test: pre-fix silent, post-fix fail-closed); chat.converse excluded from gateway grants (still L1 human). 33 pytest pass, drift 0, no contract change, containment unchanged. PASS area re-review. **S20 passed** (security cycle1 + area cycle2). Merged 5ef8fb5 (NO conflict; verified go build+tests, contracts:drift 0, migration 0005 reversible on isolated DB). Unblocks S21 (Python non-[C]) + frees [C] slot. Registry tool bodies fail-closed stubs → S21/S23. Merge BY SHA 24f54f9. CF: O1 LLM `/chat` has no self-auth (relies on gateway auth + internal network isolation) → add internal-auth/allowlist before plane is network-reachable (transport-wiring step; owners python_llm_evals + platform_reliability); O2 free-text summary numeric grounding → S22 §12.2. |
| S21 | Intent + deterministic context resolver | passed | 1 | worktree-agent-ab4c72e5 | c9bb228 | PASS (llm-plane), containment verified 2 ways (route gate + zero-tool classifier), resolver pure, digits mirror locale, fixtures 200/100 pending_native_review. Merged (90 pytest pass on merged tree). CF: `uv sync --group dev` is root-workspace not services/llm (runbook nit); S23 must honor may_use_tools in tool-binding. Unblocks S22 |
| S22 | Response contract + grounding | passed | 2 | worktree-agent-ac020c35f94297fa7 | 1c6b1ab | worker 3e443bd: typed §12.2 ResponseEnvelope (7 kinds)+SourcedValue/SourceRef, walking grounding validator, fail-closed composer, money reuses S20 Money (never float), state terms canonical, 20-row cap, retry-once via S20 node loop. Area review cycle1=CHANGES_REQUESTED: 2 blockers — (1) digit/source-ref check scanned ONLY model_inference → unsourced numbers reachable via table cells, Claim/Recommendation.statement, Calc/Comparison.label, missing_data, table summary (proven: EN+FA+Arabic-Indic all accepted); (2) EvidenceRef.quality unconstrained (any string passed CHAT-022). Fix cycle2=1c6b1ab: _check_no_digits across ALL model-visible free-text slots (ASCII+U+06F0-06F9+U+0660-0669), CANONICAL_QUALITY_KEYS subset-of-CANONICAL_STATE_KEYS+catalog-backed, +ValidationError→CannotAnswer(ENVELOPE_MALFORMED), +TABLE_ROW_COUNT_MISMATCH. Re-review cycle2=PASS: both CLOSED, no over-block, no new bypass, money-never-float intact, fail-closed strengthened. **S22 passed**. Merged f826927 (no conflict, py:test 133 pass + ruff/mypy clean on merged tree). Unblocks S23[C]. CF: numeric InlineTable cells now fail closed until S23 extends InlineTable to sourced cells (SourceRef-per-cell); CANONICAL_QUALITY/STATE_KEYS are subset allow-lists (drift test guards removal/rename, not additions); SourcedValue raw= (non-numeric evidence text) is not source-checked by design |
| S23 | Chat flows: briefing/investigation/simulation/blockers/monitoring [C] | passed | 1 | worktree-agent-a06ac4c2ae38b7ea9 | ba5cbcc | worker 142cb6d (based 6276f19, holds [C] slot). Built deterministic model-free flows layer services/llm/src/llm/flows/ (briefing/investigation/simulation/actions/blockers/monitoring) + 60-case adversarial suite + registry extension. task ci:local EXIT 0, py 166 pass, mypy strict clean, drift 0. DUAL review cycle1 BOTH CHANGES_REQUESTED. SAFETY: all 6 containment never-cut invariants VERIFIED individually + flows layer PROVABLY INERT (dark, imported by nothing, no live path) — read all 60 adversarial cases genuine (authority-claim/explicit-bypass/Persian-mixed) → 0 transitions; Draft-set exactly {draft.recommendation,draft.level2_proposal,draft.selection_set}; simulation structurally unapprovable; retry mirrors EXE-003; Draft-terminal. DOMAIN: decisive scope ruling S23 NOT complete — unwired Python island. BLOCKERS (both agree wiring belongs in THIS [C] slot — no valid downstream: registry stub says wired_in:S21/S23, S24=offline eval harness not chat wiring): B1 incomplete [C] seam — contain()/flows/DraftPort referenced by NOTHING (grep llm.flows 0 refs outside pkg), contract UNTOUCHED despite [C], no gateway briefing-fetch/chat-card endpoints, no River briefing job, DraftPort still S20 fail-closed stub → wire contain into LangGraph turn node + real DraftPort over gateway client + add gateway endpoints to contract+regenerate + River briefing job (Go services/core, migration 0014+ if store table) + cross-boundary tests. B2 byte-match guards TAUTOLOGIES (both: F1) — CHAT-070 blocker-order + CHAT-033 screen-equality assert Python literals against themselves not Go source/real screen query → §4.6 policy-order invariant can't detect Go↔Python drift → real cross-language golden emitted from Go constants. B3 adversarial suite builds IntentClass from fixture LABEL, never routes message through real classifier → proves containment-given-correct-classification only → route case[message] through real intent router + resolve pending_native_review Persian labels. B4 no free-text-containment metric on contain() boundary → emit (never-cut observability). NON-BLOCK N5 deep-link literals→single IA map, N6 CHAT-073 group_by_state doesn't segregate terminal vs in-flight. Fix packet → worker cycle1 (resumed). Cross-plane note: B1(c)(d) Go gateway/River is out of python_llm_evals charter — worker authorized to attempt under Go conventions, or hand back partial naming remaining Go slice → I dispatch Go help onto same branch. [C] slot HELD. Safety F2: [C]-tag-vs-zero-contract question resolved by completing wiring (step becomes properly [C]); no product escalation needed. CYCLE1 PARTIAL→8f8a200: Python charter COMPLETE — B1a containment on LIVE /chat turn node (approve/confirm→contain→guidance-only, never reach agent, SSE-proven); B1b GatewayDraftPort real httpx fail-closed Draft-only; B2 tautologies REPLACED (Go order_golden_test.go emits engine constants→contracts/fixtures/blocker_order.json; Python guard asserts SAME fixture; +CHAT-033 investigation_query.json screen-query golden); B3 adversarial 60 cases now MESSAGE-through-real-classifier+live TurnGraph→0 transitions metric-fired-60x; B4 llm_free_text_containment_total metric; N5 deep_links map; N6 group_by_state terminal/in_flight. py179 pass mypy67 clean test:all EXIT0 drift0. GO SLICE dispatched→go_domain_executor a06ac4c2ae38b7ea9 (based 8f8a200): B1c 4 additive gateway endpoints /chat/cards/{recommendation-draft,selection-set-draft,level2-proposal}+/briefing+regen; B1b-Go handlers reuse S17 CreateCard/NewDraft terminal-at-Draft +L2 atomic-audit; B1d River briefing job reuse internal/event Rank (IDs/order==Today CHAT-010)+store migration0014; B1e cross-boundary tests. Coupling: contract regen→strict-server iface→handlers same commit. Persian labels→persian_localization_ux (LOC-003 gate). CHAT-033 golden: web query builder must consume same investigation_query.json (web follow-up). [C] slot STILL HELD (Go slice within same step). GO SLICE COMPLETE→147205c (go_domain_executor a06ac4c2, based 8f8a200; resumed after account session-limit, finished briefing store/River). Full ci:local EXIT0 w/ live PG16 (drift0, money-guard0, race suite, migrate:verify up/down/up 0014 WITH level2 audit row). Added: 4 additive gateway endpoints /chat/cards/{recommendation-draft,selection-set-draft,level2-proposal}+GET /briefing (snake_case, opaque-string versions matching GatewayDraftPort) +regen gen/{go,py,ts}; httpapi/chat_drafts.go handlers reuse S17 mintDraftCard (refactored from CreateCard) TERMINAL-AT-DRAFT no-control; NEW machine-auth wiring kindGatewayDraft + perm.GatewayCan(computed allowlist L1-reads-minus-surface + 3 draft.* only) + kindProtected no-session bearer fallback (LLM_GATEWAY_TOKEN constant-time, never logged); PrepareLevel2Proposal proposal+audit atomic one-tx fail-closed; internal/briefing reuses event.Service.Today/Rank (IDs/order==Today CHAT-010) idempotent UNIQUE(account,business_day) ON CONFLICT DO NOTHING; River briefing job in jobs.go; migration 0014 append-only briefings/briefing_events/level2_proposals + AUD event_type+=level2_proposal. DUAL RE-REVIEW cycle2 (full step 2f3602d..147205c) BOTH PASS. DOMAIN: cycle1 incomplete-seam blocker CLOSED (contract additive drift0, endpoints policy-covered TestEveryGatewayRouteHasPolicy, River idempotent reuse-Rank, DraftPort shapes match handlers field-for-field). SAFETY GATE #1 machine-token-cannot-approve/execute VERIFIED 4 defense layers (GatewayCan computed allowlist every L2/L3/L4 absent-by-construction; middleware 403 on confirm/bulk-confirm/execute/retry; no state-advance route mislabeled L1; constant-time compare never-logged) + S8 default-deny not regressed + live containment 60/60→0 transitions + Draft-terminal + L2 atomic append-only + briefing determinism + no-float. Merge 77a5733→go.mod-tidy-fix ba5cbcc (pre-push go-tidy caught oapi-codegen/runtime indirect→direct promotion from S23 gen/go usage; committed tidy, re-push all-green). NEW S35 GATE precondition: LLM_GATEWAY_TOKEN must be strong rotated deploy secret before S34 (fails-closed if empty=safe). NON-BLOCK follow-ups: (1) empty EvidenceVersions on chat Drafts (mirrors S17 persistence; track S17 extension→per-observation version map so APR-001 evidence-invalidation directly backed); (2) session-principal 401-vs-403 on kindGatewayDraft (status nicety, fails closed); (3) daily briefing = point-in-time snapshot can diverge from live Today intraday (product note). Python-side cycle1 fixes (containment-on-live-turn, real classifier adversarial 60/60, cross-language goldens, observability metric) all verified wired. **[C] SLOT FREE.** Unblocks S24(S21,S22,S23) + S29 chat-dock(S25,S23). |
| S24 | Eval harness + eval sets | pending | 0 | — | — | paid benchmark deferred; phase-C boundary |
| S25 | SPA foundation + i18n + pseudo-locale gate | passed | 1 | worktree-agent-a4d3322c | 757c1b21 | PASS: web-surface area + locale-qa BOTH cycle 1. Merged 8761e8e (clean; verified ts:lint/test/pseudoloc + contracts:drift 0 post-merge w/ S8/S9 gen). Vite8/React19/TanStack shell, fa-IR verbatim §11.4 terms, Jalali-over-UTC (leap-verified via Intl), source-unit-only money, copylint+pseudoloc gates live in CI (S6 placeholder removed), Sentry→Spotlight tree-shaken from prod. CF (non-block): LOC-010 lang-switcher hardcodes 2 locales→data-drive; @sentry/react→devDependencies; wire assert-prod-clean into ci:local (later web step); add Tehran-tz Jalali boundary row. **LOC-003 native Persian sign-off = release gate (deferred list).** Route bodies are scaffold EmptyStates → S26–S28 fill |
| S26 | Screens: onboarding/connection, Products, Product detail, Cost import | passed | 2 | worktree-agent-af1bd75c479ade85b | eb225f9 | worker cb8d05c→fix 2fe3250 (isolated, resumed after session-limit). NON-[C], apps/web+packages/locale only. DUAL area review: web-surface PASS cycle1; locale-qa CHANGES_REQUESTED cycle1 → PASS cycle2. Journey-1 screens per design consuming gen/ts (NO contract edit). ACC-001 CapabilityGate single enforcement point (state==supported; Unknown/Unsupported/Degraded stay disabled + gatedNote) w/ negative test. Money source-unit-only via packages/locale/format/money (bigint scale no float; currency!=region.sourceCurrency quarantines; Toman NOT inferred — Gate 0a). Raw marketplace prices render raw LTR text never promoted to computed Money. NO fabricated values (owned-offer unavailable / contribution placeholder / cost notRecorded — never 0). Identity quarantine via structured confirm/reject/defer (free-text note = audit evidence, no authority). CSV import mirrors S12 contract (preview→per-row disposition→commit-on-batchId; dup blocks commit; negative test no-commit-while-dup). LOC blocking fix cycle2: native DK identifiers now render raw LTR (String(nativeVariantId/nativeProductId) in LtrToken, no Intl/grouping/digit-family) matching supplierCode/sku/evidenceRef — closes Products search desync (displayed==haystack); back-link '←' hardcode removed → catalog key product.back (fa-IR right-arrow, en left-arrow, direction follows locale). Merge eb225f9 (--no-ff; both reviews PASS). CF (non-block): (1) Playwright journey1 e2e authored+harness-proven but authenticated run deferred to CI; (2) contribution/owned-offer placeholders until later endpoint step; (3) AppLink thin shim isolating single `never` cast over S25 untyped router; (4) muted relevance not yet wired to feed filter (S15 scope). |
| S27 | Screens: Today/event/recommendation/approval | passed | 1 | worktree-agent-ad60398bc8c39a9e9 | da0a7b9 | worker a16c4ee(locale)+b02799d(web), based 5b32d41 (web/locale only, non-[C]). DUAL review BOTH PASS cycle1 — web area + safety. Today (EventRow ranked exposure×confidence×urgency all 3 factors visible, blocker chips deep-link IA map, loading/empty/error/blocked states), event detail (4-way EvidencePanel by kind/age/quality/materiality-threshold-version, EVT-005 known-Money-or-explicit-Unknown), recommendation (PRC-001 every field present-or-unavailable-with-reason, ContributionBreakdown, allowed range), ApprovalCard (14 fields, structured confirm = ONLY mutation trigger), StateMachineView (§8.4 incl 8 revalidation gates + Invalidated/Expired/permission-denied/recommend-only). SAFETY verified 6 never-cut adversarially: structured-confirm-only (single type=button, NO form/submit/onKeyDown; Enter-on-body/card does NOT fire onConfirm; payload carries binding actionId+param/context/policy/cost versions+evidenceVersions+expiresAt, edited price never travels); APR-001 invalidation UX (voided=edited||staleVersion, canConfirm gated, polled version-change disables+recalculate, no stale-submit race); simulations carry no control (live only when awaiting_confirmation && hasControl); PRC-001 optionality never blank-as-zero; localization glossary verbatim (state terms match design/README.md, native IDs raw LtrToken, logical CSS); no client authority leak (StateMachineView driven by server result, permission_denied 403 fails closed no downgrade). WEB AREA verified consumer discipline (only gen/ts, no contracts/gen edits), component inventory fidelity, STATE_MATRIX coverage, deep-link map. Merge da0a7b9 (--no-ff, no conflict — disjoint from S18[C]). Merge-verify: drift 0, ts:lint 104 files clean, pseudoloc/copylint + 36 web tests green, pre-push contracts-drift/go-lint/go-tidy/mypy green. NON-BLOCKING carry-forward (all within S27 spec, none violate never-cut): (1) ContributionBreakdown built+unit-tested but dead in prod until API surfaces Contribution → api_data_contracts; (2) dead useEventRelevance hook → wire in S28 relevance UI or drop; (3) Today connector-disconnected STATE_MATRIX state collapses to generic error → S28/shell; (4) PRC-001 fields render unavailable-with-reason b/c P0 gateway has NO recommendation-detail endpoint (no Recommendation schema; card lacks objective/current-price/contribution/allowed-range/quality/readiness/assumptions) — WITHIN spec, escalate contract gap to api_data_contracts; (5) edit-price 'new version via API' degrades to client void+refetch (no edit-price endpoint) — never-cut holds (edited price voids control, never travels to confirm), rides same api_data_contracts follow-up. SAFETY note→S18: post-confirm button re-enables before invalidating refetch → double-click could POST /confirm twice; server EXE-002 idempotency key covers, but S18 must verify zero-dup-on-race end-to-end (folded into S18 safety packet). |
| S28 | Screens: market/actions/bulk/settings/ops | passed | 1 | worktree-agent-a2d650d026d7b3c39 | 0644e78 | worker b211025 (based 6276f19, web+locale only non-[C]). Market/Actions/BulkApproval/Settings/Operations + BulkToolbar/CoverageBars/QueueCard + disposition.ts. ts gates green (18 files/53 tests, lint 0, pseudoloc/copylint 50 files, build); ci:local red ONLY at migrate:verify (no DB in worker sandbox; S28 adds NO migrations→environmental, push-CI clears). WEB AREA PASS + SAFETY PASS. Safety traced bulk seam client→gateway ConfirmBulkApproval→BulkPreviewValid, all 7 invariants: bulk stale-preview race CLOSED (version diverges synchronously, button disables same render, payload carries boundVersion); blocked never force-included (payload only {lineage,boundVersion} not item list); retry ABSENT for pending_reconciliation (EXE-003 MSW neg); L3 edit owner-only RENDER-gated (absent for Operator) + Operations internal-gate fail-closed; client-synthesized lineage SAFE — real server 404s unregistered lineage (GetCurrentSelectionSet ErrNoRows) so APR-001 set-binding uncforgeable client-side; recommend-only executionPending never claims executed write. Web area: consumer discipline (only gen/ts; 6 contract gaps unavailable-with-reason per S27), component inventory, STATE_MATRIX, glossary verbatim. LOCALE-QA review PASS (495/495 key parity, all canonical glossary terms verbatim, natural operator Persian, bidi/digits/slots correct). ALL 3 REVIEWS PASS → merge 0644e78 (--no-ff, no conflict — web/locale disjoint from S23 Go). Merge-verify drift 0, ts:lint 120 clean, pseudoloc, pre-push go-lint/go-tidy/mypy green. 4 non-block LOCALE polish nits (future whole-catalog harmonization pass, incl pre-existing S27 drift): operations.queue.staleTargets قدیمی→قدیمی‌شده; actions.audit.title رد-ambiguity→پیشینهٔ ممیزی; actions.audit.card gerund→تصویر لحظه‌ای; SKU column terminology variance across catalog. NON-BLOCK carry-forward: (1) preview version-gate no async-disposition fingerprint — close before bulk-execute enabled; (2) client lineage must become server-minted (CreateSelectionSet unexposed)→api_data_contracts; (3) freshness fixed-thresholds vs per-offer freshnessDeadline; (4) MSW handler bulkValid unconditional (proves client SENDS version not server binding); (5) onApprove no onError; (6) Market refresh→/connector/refresh not observation-refresh. 6 contract gaps→api_data_contracts backlog. |
| S29 | Chat dock UI | pending | 0 | — | — | phase-D boundary: safety review |
| S30 | Extension: pairing + passive capture | pending | 0 | — | — | [C] only if pairing endpoints added |
| S31 | Extension: overlay/watchlist/scheduled | pending | 0 | — | — | phase-E boundary: safety review |
| S32 | Integration + adversarial + kill-switch suites | pending | 0 | — | — | lists non-automatable §16 rows here |
| S33 | Observability + dashboards + runbooks | pending | 0 | — | — | phase-F safety review with S34 |
| S34 | Production deployment | pending | 0 | — | — | **GATED: live — human go** |
| S35 | Gate 0a live probes + parameter verification | pending | 0 | — | — | **GATED: live+paid — human go; pullable earlier once production access exists** |
| S36 | Internal alpha sign-off | pending | 0 | — | — | **HUMAN sign-off** |

> Status values: pending | in_progress | passed | blocked. Note carries one line: review outcome, test counts, CARRY-FORWARD constraints a downstream step must honor, or why blocked.

## Dependency graph

```
Phase A  S1 → {S2, S3} ; S3 → S4[C] ; {S2,S3} → S5 ; S3 → S7 ; {S4,S5} → S6
Phase B  {S4,S5,S6} → S8[C] ; {S4,S5} → S9[C] ; S9 → S10 ; S10 → S11[C] ; S10 → S12[C]
         {S5,S11} → S13[C] ; S13 → S14 ; S13 → S15[C] ; {S7,S12} → S16[C]
         {S15,S16} → S17[C] ; {S9,S17} → S18[C] ; {S15,S18} → S19[C]
Phase C  {S4,S8} → S20[C] ; S20 → S21 ; S21 → S22 ; {S22,S15,S17} → S23[C] ; {S21,S22,S23} → S24
Phase D  {S4,S6} → S25 ; {S25,S8,S10,S11,S12} → S26 ; {S25,S17} → S27
         {S26,S27,S18} → S28 ; {S25,S23} → S29
Phase E  {S8,S13} → S30 ; {S30,S14} → S31
Phase F  {S18,S24,S28,S29,S31} → S32 ; {S2,S18,S19} → S33 ; {S32,S33} → S34(GATED)
         {S9,S13,S14,S16,S18,S24}+prod access → S35(GATED) ; {S32,S33,S34,S35} → S36(HUMAN)
```

Parallel tracks after Phase A: Go domain chain (S8–S19), Python chain (S20–S24), web chain (S25–S29), extension chain (S30–S31) — subject to the [C] mutual exclusion.

## Log
> Append-only. One line per state change: what passed/blocked, merge SHA, what's next.
- 2026-07-16: Docs authored and progress seeded (S1..S36 = pending). Next: orchestrator SETUP, then S1.
- 2026-07-17: SETUP done. 11 agent profiles loaded; integration branch `dk-p0/main` created off `acce0c7`. Dispatched S1 (worktree isolation, capability role connector/reliability→reliability-delivery for scaffold; reviewer reliability-delivery area charter platform_reliability). Verification commands don't exist pre-S1; S1 bootstraps them.
- 2026-07-17: S1 PASSED (1 cycle, reviewer independently reproduced Verify) → merged into dk-p0/main (worker fd58883). `task ci:local`/toolchains confirmed live. Eligible now: S2 (dev stack) + S3 (Go core skeleton) — both depend only on S1, disjoint files, neither [C] → dispatched concurrently. Next after: S3→S4[C], {S2,S3}→S5, S3→S7.
- 2026-07-17: RECONCILED from git after compaction — S1 merged+passed (a29e384). S2 (c88c599) & S3 (2bf32cf) workers done, pending review. S2 hit Docker-egress block; human authorized native PG16.13 install (Docker+apt.postgresql.org both blocked) — DB path now live-verifiable, Docker observability boot deferred to gate. Cleaned duplicate seed rows. Next: review S2 + S3 concurrently.
- S22 passed+merged (worker 3e443bd→fix 1c6b1ab; merge f826927). llm-plane area cycle1 caught free-text-containment hole (digit ban only on model_inference; unsourced EN/FA/Arabic-Indic numbers leaked via table cells + claim/recommendation statements + labels + missing_data + summary) and unconstrained EvidenceRef.quality (CHAT-022). Fixed: digit-ban all model-visible free-text slots + canonical quality keys + ValidationError fail-closed. Re-review PASS. Merge-verify 133 pytest + ruff/mypy clean. Unblocks S23[C]. S13[C] still building (single [C] slot occupied).
- S13[C] dual review cycle1: BOTH area(connector-obs)+safety(never-cut) CHANGES_REQUESTED. Spine held (OBS-001 trigger unbypassable, append-only, money-raw, dedup route-keyed, 0007 reversible). 3 BLOCKING on quality-derivation/Route-B boundary: (1) upload identity fail-open + client self-cert quality; (2) Verified from stale out-of-window route (no per-route timestamp in model); (3) Conflicted unreachable → §16 route-disagreement fails open. +untruthful 0007 comment (no target-retire on identity-reopen) +Conflicted gate mismatch. Consolidated 1 fix cycle → worker (a393d340809895aa2). S13 still holds the single [C] slot; S23[C]/S12[C]/S15[C]/S16[C] all blocked until it merges.
- S13[C] passed+merged (worker ea38280→fix 96dd3e3; merge 6120acd). Dual review 2 cycles both PASS. Cycle1 caught 3 BLOCKING never-cut gaps (Route B identity fail-open+client self-cert; Verified via stale out-of-window route; Conflicted unreachable/§16 fail-open) — all fixed via append-only in-window corroboration derivation + server-authoritative identity/quality + reachable Conflicted. Merge-verify drift 0 + tests green. **[C] slot FREE**; observation store live → S14 (Route C observer) + S30 (extension) open; S23[C]/S12[C]/S15[C]/S16[C] contend for [C] slot. 16 steps passed (S1-S11,S13,S20,S21,S22,S25).
- INCIDENT (recovered): dispatched S12/S14 workers WITHOUT isolation:worktree → they ran in the SHARED main checkout; S12 worker's base-sync `git reset --hard origin/dk-p0/main` moved the dk-p0/main REF from d03f6da back to fd1e42c (branch refs are shared across worktrees), dropping the S13 merge from the branch. Commits (6120acd merge, d03f6da progress) survived as objects. Recovery: TaskStop both workers (no commits yet), `git reset --hard d03f6da` (while dk-p0/main checked out), delete stray s12-work branch, verify 6120acd ancestor + observation files present, push d03f6da to both branches. Re-dispatched S12/S14 WITH isolation:worktree + worktree-discipline guardrails (never reset/branch -f/checkout/push shared refs; rebase not reset to sync). RULE: EVERY worker/review dispatch MUST set isolation:worktree.
- S12[C] passed+merged (worker dfa40f6; merge 25fc1e8). Dual review both PASS cycle1. Money integer-only verified adversarially (>2^53, overflow, over-precision all rejected). Merge-verify drift 0 + tests green. **[C] slot FREE** → S15[C]/S16[C] eligible. CF: SYSTEMIC int64-mantissa-as-JS-number → api_data_contracts. // S14 area CHANGES_REQUESTED cycle1 (safety PASS): backoff dead-config + budget no-reset starvation + redirect host-pin → 1 fix cycle to worker. 17 steps passed (S1-S13,S20,S21,S22,S25 + S12).
- S14 passed+merged (worker 9a265c6→fix 9cb537d; merge bab6853). Area cycle1 caught 2 BLOCKING: backoff dead-config + budget permanent-starvation (no daily reset); +safety-flagged redirect host-pin. All fixed cycle2 (wired retry honoring all guards; deterministic 24h window reset; CheckRedirect). Re-review PASS. sqlc querier.go conflict with S12 cost resolved by regeneration. Merge-verify migrate→10 + build + routec/observation/cost tests + drift 0. 18 steps passed (S1-S14,S20,S21,S22,S25). S16[C]+S26 still building; S14 fix done. Migrations now merged: 0008(cost) 0010(routec); 0009 reserved for S16[C].
- INFRA/session-limit: S16[C] + S26 workers BOTH died to session-limit (resets 15:50Z) mid-build; worktrees preserved → resume via SendMessage after reset (a8da2de4f254a4aaa=S16, af1bd75c479ade85b=S26). // EXTERNAL: maintainer merged PR #36 (ci: cache Go tools + CLAUDE.md/README docs) directly onto origin dk-p0/main @ 12ba050 — FF-integrated into local (44f79ef→12ba050), mirror synced. PR #2 head=dk-p0/main @ 12ba050, CI GREEN both runs, mergeable clean, no review comments. 18 steps passed (S1-S14,S20,S21,S22,S25). S16[C] holds [C] slot (interrupted). Next after S16 merges: S15[C]. NOTE: CLAUDE.md changed by #36 (+12 lines contributor workflow docs) — additive, re-read if needed.
- S16[C] passed+merged (worker fa8c41d; merge 89205b6). Dual review both PASS. Property tests 10k/0-counterexample (ordering+zero-floor), simulation non-approvable structurally, money never-float. Merge-verify drift 0 + money-guard + props green. **[C] slot FREE**. 19 steps passed (S1-S14,S16,S20,S21,S22,S25). Dispatched S15[C] event engine (go_domain_executor, migration 0011) — floor detector now consumes live S16 outputs. S26 web still building (resumed). Next after S15 merges: S17[C] (needs S15+S16 — both then done).
- S26 passed+merged (worker cb8d05c→fix 2fe3250; merge eb225f9, --no-ff). Web-surface area PASS cycle1 (ACC-001 gating, money source-unit-only+quarantine, no-fabricated-values, identity quarantine all clean). locale-qa CHANGES_REQUESTED cycle1: BLOCKING — native DK identifiers run through locale number formatter (grouping+Persian digits) → breaks LTR-technical-identifier rule + desyncs Products search haystack vs displayed cell. Fixed cycle2 (raw String() in LtrToken; back-arrow → catalog key) → locale-qa PASS cycle2. apps/web+packages/locale only, NO contract/gen change, drift 0. 20 steps passed (S1-S14,S16,S20,S21,S22,S25,S26).
- INFRA/toolchain-repair: container refresh lost Go tools → `task doctor` reported MISSING: oapi-codegen (the v2.8.0 generator bumped by maintainer #58 'dedup CI + upgrade toolchain'). This wedged the pre-push contracts:drift hook 10+min (go run @v2.8.0 stalling through proxy). FIX: go install oapi-codegen@v2.8.0 (→/root/go/bin, added to PATH + ~/.bashrc+~/.profile so hook subshells inherit) → doctor All-tools-present. Second snag: merge-regenerated lockfile left node_modules stale; msw (S26 test-mock dep) had no allowBuilds decision → pnpm wrote placeholder in pnpm-workspace.yaml + exit1. FIX: committed msw:false (6621fee; node/vitest MSW needs no build script) + CI=true frozen install reconcile. contracts:drift now exit0 end-to-end.
- EXTERNAL: maintainer merged PR #58 'chore(repo): deduplicate CI and upgrade toolchain' onto origin dk-p0/main @ a44b040 (diverged from local — both had commits past 5f0a23f). Integrated via merge 4302eeb (only pnpm-lock.yaml conflict → regenerated clean, supply-chain policy passed; #58 touched ci.yml/AGENTS.md/Taskfile/biome.json/gen additively). MIRROR RETIRED per user directive 'merge steps into dk-p0/main only, we don't need both' — henceforth push ONLY dk-p0/main (mirror claude/dk-p0-orchestrator-788qtr left stale on origin @ dfd86e6, not maintained).
- S15[C] passed+merged (worker e096d91→fix 033dc29; merge 810277f). SAFETY PASS cycle1, DOMAIN PASS cycle2 (blocker: missing httpapi 503 negative test → added). gen/go conflict w/ #58 resolved by regenerate (drift 0). Pushed 810277f — pre-push merge-verify ALL GREEN (contracts-drift/go-lint/go-tidy/mypy). **[C] slot FREE** → S17[C] eligible (needs S15+S16, both done). 21 steps passed (S1-S16,S20,S21,S22,S25,S26). Next: dispatch S17[C] (recommendations + approval state machine — money/approval-versioning [C]).
- S17[C] passed+merged (worker 20f6f78; merge 1600923). DUAL review BOTH PASS cycle1 — domain-execution area + safety never-cut (approval versioning APR-001, structured-only approval, LLM-plane isolation, PRC-002 zero-controls, idempotency, append-only, money no-float, fail-closed S18 stub — all VERIFIED adversarially vs live code/SQL). No conflict (origin static at 7982837). Pushed 1600923 — pre-push merge-verify ALL GREEN (contracts-drift 5.6s, go-lint 0, go-tidy, mypy). **[C] slot FREE**. 22 steps passed (S1-S17,S20,S21,S22,S25,S26). CF→S18: server-resolve `current` at Revalidating (not client echo); bulk per-member re-validate. Next eligible: S18[C] (needs S9,S17 — both done), S23[C] (needs S22,S15,S17 — all done), S27 (needs S25,S17 — both done, non-[C] web). [C] slot serializes S18/S23 → dispatch S18[C] + S27 (web, no [C]) concurrently.
- S27 passed+merged (worker a16c4ee+b02799d; merge da0a7b9). Web/locale only, non-[C] — merged while S18[C] under review (disjoint: web vs Go/contracts). DUAL review BOTH PASS cycle1 (web area + safety; safety verified all 6 never-cut incl structured-confirm-only + APR-001 invalidation UX adversarially). Merge-verify drift 0 + ts:lint 104 clean + pseudoloc/36-tests + pre-push all green. **23 steps passed** (S1-S17,S20,S21,S22,S25,S26,S27). 5 non-blocking carry-forwards logged (contract-gap fields unavailable-with-reason = within spec, escalate recommendation-detail+edit-price endpoints to api_data_contracts; dead useEventRelevance→S28; Today connector-disconnected state→S28/shell). S18[C] (execution/audit) still under domain+safety review — [C] slot held. Next after S18 merges: S19[C] (needs S15,S18) + [C] frees for S23[C]; S28/S29 web (need S26/S27).
- S15[C] reviews in: SAFETY co-review PASS (every never-cut structurally enforced — verified via live-PG16 direct-SQL probes: dedup partial-unique-index rejects dup raw INSERT; EVT-005 dual CHECKs reject numeric-when-unknown + known-without-currency; no-float; append-only tables no UPDATE/DELETE; free-text note never reaches Rank(); read.events L1 + relevance L2 not approve/execute). 2 non-block product-ranking notes → product_delivery_lead (Unknown-exposure band ranks below ALL known regardless of severity; muted not wired to feed). RECORDED 2026-07-17 as PD-1 + PD-2 in `docs/implementation/dk-p0-product-decisions.md` (new §21 product-decisions/ranking-policy register), both non-blocking/needs-product-decision, tagged Today feed, code as-built @ 810277f — hold no step/gate; PD-1 defer decision to Phase D, PD-2 semantics decide-now (recommend demote) + wire in Phase D. DOMAIN area review CHANGES_REQUESTED cycle1: domain core CORRECT (ranking big.Int no-float, detectors, dedup, EVT-005 struct, migration 0011 reversible, perm wiring all pass) — single BLOCKER = missing httpapi negative test (event handlers return 503-when-unwired but NO event_test.go while every sibling plane has one). Fix packet → S15 worker (add event_test.go: 503 for GET /events,/event,/today + POST /events/relevance unwired + 1 wired happy-path mapping assert). S15 still holds [C] slot.
